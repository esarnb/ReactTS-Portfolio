export interface singleCard {
  title?: string,
  desc?: string,
  img?: string, 
  alt?: string,
  color?: string
}

export interface GitData {
  title: string,
  desc: string,
  padding: string,
  shadow: string,
  radius: string
}